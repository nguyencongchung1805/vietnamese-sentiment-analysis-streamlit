# Vietnamese Sentiment Analysis

NLP project for classifying Vietnamese product reviews using:

- SVM (TF-IDF)
- BiLSTM (PyTorch)
- PhoBERT (Transformers)
- Streamlit UI

## Features
- Single review prediction
- Batch CSV prediction
- Model comparison

## Run app
```bash
streamlit run app.py